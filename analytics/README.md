# Analytics Workspace

This folder is reserved for reproducible analytical queries, notebook exports, and metric definitions.

Initial metrics are generated by `etl/pipeline.py` and exposed in:

- `datasets/warehouse/fact_narrative.csv`
- `datasets/warehouse/fact_sentiment.csv`
- `datasets/warehouse/fact_entity_mentions.csv`
- `datasets/warehouse/fact_trends.csv`
